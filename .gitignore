# --- Dark Sol repo rules ---
# Ignore everything by default; explicitly allow only the main script (and this .gitignore).
*
!.gitignore
!Dark Sol.py
!lib/
!lib/**

# i want the entire lib folder to be included
